#pragma once

#define STELLAR_VERSION       "@PROJECT_VERSION@"
#define STELLAR_VERSION_MAJOR  @PROJECT_VERSION_MAJOR@
#define STELLAR_VERSION_MINOR  @PROJECT_VERSION_MINOR@
#define STELLAR_VERSION_PATCH  @PROJECT_VERSION_PATCH@

// Build timestamp is in AppBuildTime.h, regenerated on every build (not just
// every cmake configure) so the date shown in About is always accurate.
#include "AppBuildTime.h"
