Use site patterns in categories (e.g., *.youtube.com) to auto-route downloads by domain, not just file type.
Enable "Automatically start downloading" in Settings > Browser to intercept downloads without showing the dialog each time.
Drag downloads from the table to a category in the sidebar to move them—no need for the menu.
Use "Download Later" to queue files without starting them, useful for planning large batches before bandwidth is available.
Set different save paths per category so videos, music, and archives don't all land in the same folder.
Rejected intercepted downloads twice? Stellar offers to add the URL to your exception list, so it won't bother you again.
Segmented downloads split large files across multiple connections-check Progress > Segments to see the parallelization.
The speed limiter prevents Stellar from saturating your connection; use it to reserve bandwidth for other tasks.
Right-click downloads to open the folder-faster than hunting in Explorer.
Set a global default save path in Settings, or override it per-category for automatic organization.
Hold your configured modifier key (Alt/Ctrl/Shift) when clicking a download link to let the browser handle it instead of Stellar intercepting it.
Ctrl+Shift+N opens the batch download dialog for adding multiple URLs at once.
Ctrl+I opens the statistics dialog showing all-time downloaded bytes, upload ratio, and seeding totals.
Create named queues with independent speed limits and concurrency settings—useful for separating work downloads from personal ones.
The queue scheduler lets you set start/stop times and days of week, so downloads only run during off-peak hours.
Drag category and queue entries in the sidebar to reorder them.
Drag the sidebar section headers to reorder entire sections (Categories, Torrents, Queues, Grabber).
Per-download speed limits can be set in the progress dialog without affecting the global limit.
Open properties on a torrent and toggle individual files or folders off so they never download.
Right-click a torrent to copy its magnet link or export the .torrent file.
The Grabber crawls a website and extracts all matching download links in bulk.
Clipboard monitoring in Settings > General lets Stellar detect URLs you copy and prompt to download automatically.
The public IP indicator in the status bar lets you confirm your VPN or proxy is actually active before downloading.
Bind Stellar to a VPN interface in Settings > Torrents, so torrent traffic is blocked entirely if the VPN disconnects.
Auto-ban rules in Settings > Torrents block abusive or media-player peers automatically during seeding.
Rename a torrent's top-level folder by right-clicking it and choosing Rename.
Enable "Speed in title bar" in Settings to see your current download speed at a glance.
Column headers in the download table can be dragged to reorder and resized.